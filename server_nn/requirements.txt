# Python 3.13.11 is the source environment. Install CPU torch separately first:
# python -m pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
# Public versions are pinned; the checkpoint compatibility bridge also checks
# source/data identities and reconstructs predictions on the receiving machine.
numpy==2.2.6
pandas==2.3.3
torch==2.14.0
scikit-learn==1.8.0
scipy==1.16.3
joblib==1.5.3
threadpoolctl==3.5.0
filelock==3.20.0
psutil==7.0.0
matplotlib==3.10.8
